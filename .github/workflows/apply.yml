name: cicd
on: [push]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: verify-terraform
      run: terraform --version
    - name: validate
      run:
        /home/runner/work/bootstrap-serverless-app/bootstrap-serverless-app/.ci-scripts/validate.sh bundles
